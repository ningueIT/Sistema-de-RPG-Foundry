// Arquivo substituído: use `generate-habilidades-macros-lutador.mjs`.
// Mantido por compatibilidade apenas; não execute este arquivo.

(function(){
  ui.notifications.info('Use o macro generate-habilidades-macros-lutador.mjs (GM) para gerar as macros de Habilidade.');
})();